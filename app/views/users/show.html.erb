<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @user.user_id %>
</p>

<ul>
  <% @user.user_messages.each do |message| %>
    <li><%= message.text %></li>
  <% end %>
</ul>

<%= link_to 'Back', users_path %>
